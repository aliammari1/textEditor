/**
 * Is a simple program that creates a new instance of the TextEditor class and calls
 * its constructor to initialize its properties.
 */
public class Main {
    /**
     * Creates a new instance of `TextEditor`.
     * 
     * @param args 0 or more command-line arguments passed to the `main` method of the
     * Java class.
     */
    public static void main(String[] args){
        TextEditor textEditor = new TextEditor();
    }
}
